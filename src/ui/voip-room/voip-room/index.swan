<rtc-room
    id="{{onekitId}}"
    class="onekit-voip-room {{onekitClass}}"
    style="{{onekitStyle}}"

    id="{{openid}}"
    device-position="{{devicePosition}}"

    binderror="voipRoom_error"
>
    <rtc-room-item
        user-id="{{openid}}"></rtc-room-item>
</rtc-room>