{
  "name": "weixin2baidu",
  "version": "1.0.1",
  "description": "Weixin-Miniprogram SDK for Baidu-Miniprogram",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/www-onekit-cn/weixin2baidu.git"
  },
  "keywords": [
    "custom-component",
    "mini-program"
  ],
  "devDependencies": {
    "rc-tools": "6.x"
  },
  "scripts": {
    "build": "rc-tools run compile && node scripts/cp.js && node scripts/rm.js",
    "pub": "git push origin && npm run build && npm publish"
  }
}

