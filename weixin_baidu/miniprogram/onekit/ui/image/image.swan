<image 
id="{{Id}}"
class="onekit-image {{onekitClass}}"
style="overflow:visible;{{onekitStyle}};{{new_size}}"
mode="{{mode}}" 
src="{{src}}"
webp="{{webp}}"
lazy-load="{{lazyLoad}}"

binderror="image_error"
bindload="image_load"
>
   <slot/>
</image> 