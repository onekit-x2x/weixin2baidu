<image 
id="{{Id}}"
class="onekit-image {{Class}}"
style="overflow:visible;{{Style}};{{new_size}}"
mode="{{mode}}" 
src="{{src}}"
webp="{{webp}}"
lazy-load="{{lazyLoad}}"

binderror="image_error"
bindload="image_load"
>
   <slot/>
</image> 